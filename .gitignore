doc/

# Internal-only docs (specs, plans, brainstorms, solutions, etc.) are kept
# locally but never tracked. Only the end-user-facing docs are whitelisted
# below; everything else under docs/ stays out of the repo.
docs/*
!docs/guide.md
!docs/why-coherence-matters.md
!docs/agent-coherence-approach.md
!docs/reproduce.md
!docs/security.md
!docs/ccs-diagnose.md

states/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
*.egg-info/
dist/
build/
.venv/
venv/
.env
benchmarks/results/latest.json

# Q6 probe verdict output (may reference conversation metadata; local-only)
examples/conversations_stale_read/q6_*.json

# 1Password secret-reference working copy (per-user vault/item names)
.env.op

# Claude Code coherence plugin local coordinator state (contains hook.secret, state.db)
.coherence/
