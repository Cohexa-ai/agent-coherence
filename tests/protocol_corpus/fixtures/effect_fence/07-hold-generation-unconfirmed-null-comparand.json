{
  "name": "effect-fence-hold-generation-unconfirmed-null-comparand",
  "description": "HOLD reason `generation_unconfirmed`, the residual bucket -- and the twin of 06. The request body here is BYTE-IDENTICAL to 06's; the only difference is that this workspace's preflight pre-read DID carry a content_hash, so the coordinator holds a real content claim and leg 5 passes. expected_generation is an EXPLICIT null, which is not a missing field but a captured FACT: 'the coordinator I read from confirmed no generation' (one too old to report them, a strict deny, a degraded read). That is representable in the fence vocabulary, so it flows through as the sentinel and HOLDS here rather than 400-ing -- contrast 11, where the same field OMITTED is a 400, because no number of identical retries can supply a comparand the caller never captured.",
  "setup": {
    "tracked": ["docs/**"],
    "files": {
      "docs/plan.md": "# coherence corpus fixture  \n"
    },
    "preflight_requests": [
      {
        "method": "POST",
        "path": "/hooks/pre-read",
        "body": {
          "session_id": "11111111-1111-4111-8111-111111111111",
          "path": "docs/plan.md",
          "content_hash": "f9b414ed170aade26e96b08b64710e279b57971274301dc1c4c22fb63de6eec6"
        }
      }
    ]
  },
  "request": {
    "method": "POST",
    "path": "/hooks/effect-fence",
    "body": {
      "session_id": "11111111-1111-4111-8111-111111111111",
      "path": "docs/plan.md",
      "expected_version": 1,
      "expected_generation": null,
      "content_hash": "f9b414ed170aade26e96b08b64710e279b57971274301dc1c4c22fb63de6eec6"
    }
  },
  "expected": {
    "status": 200,
    "body": {
      "verdict": "hold",
      "reason": "generation_unconfirmed"
    }
  },
  "backends": ["python"]
}
